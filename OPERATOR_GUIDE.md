# คู่มือเจ้าหน้าที่ PAC Ticket

เปิดลิงก์ Web App เดิม แล้วเติม `?page=admin` เช่น `.../exec?page=admin` จากนั้นใส่รหัสเจ้าหน้าที่ (ADMIN_TOKEN) ที่ผู้ดูแลระบบมอบให้ รหัสนี้อยู่ในหน่วยความจำของหน้าเว็บชั่วคราวเท่านั้น จึงต้องไม่ส่งต่อหรือบันทึกไว้ในชีต

## งานประจำวัน

- **ภาพรวม** แสดงความจุ บัตรที่สำรอง คงเหลือ จำนวนรายการ/ใบตามสถานะ และรายได้ที่นับเฉพาะรายการ `ยืนยันแล้ว` เท่านั้น กดการ์ดรอบการแสดงเพื่อดูรายการของรอบนั้น
- **รอตรวจสอบ** เรียงรายการเก่าที่สุดก่อน กด **ดูสลิป** เพื่อเปิดสลิปในหน้าต่างของระบบ แล้วกด **ยืนยัน** หรือ **ปฏิเสธ** พร้อมบันทึกเหตุผลได้
- **ยืนยันแล้ว** ใช้ตรวจสอบรายการชำระสำเร็จ และกด **ยกเลิกการจอง** เมื่อจำเป็น ระบบคืนโควตาอย่างปลอดภัยเพียงครั้งเดียว
- **ปฏิเสธ / ยกเลิก** เป็นประวัติถาวร ไม่ต้องลบข้อมูลใน Google Sheets
- **รายการทั้งหมด** ค้นหาได้จากรหัสการจอง ชื่อ หรือส่วนท้ายของเบอร์โทรศัพท์ เลือกรอบ สถานะ และลำดับได้ กด **ล้างตัวกรอง** เพื่อเริ่มใหม่

สัญลักษณ์เตือนเบอร์โทรศัพท์ซ้ำเป็นเพียงข้อมูลประกอบการตรวจสลิป ไม่ใช่เหตุให้ปฏิเสธอัตโนมัติ

## การตรวจสลิป

1. เทียบชื่อ ยอดเงิน รอบ และจำนวนบัตรกับรายการ
2. กด **ยืนยัน** หากถูกต้อง บัตรยังคงถูกสำรองและรายได้ยืนยันแล้วจะเพิ่ม
3. กด **ปฏิเสธ** หากไม่ถูกต้อง ระบุเหตุผลถ้ามี ระบบคืนจำนวนบัตรให้รอบนั้น
4. การกดซ้ำไม่ทำให้คืนโควตาซ้ำ เพราะระบบบังคับสถานะและบันทึก Audit Log

สลิปยังอยู่ใน Google Drive ส่วนตัวของระบบ อย่าทำโฟลเดอร์สลิปเป็นสาธารณะ หากเปิดภาพไม่ได้ ให้ตรวจสิทธิ์ Drive ของบัญชีเจ้าหน้าที่

## ส่งออก CSV

ตั้งตัวกรองในหน้ารายการตามที่ต้องการ แล้วเลือก **ส่งออก CSV** (เมื่อเปิดใช้งานในหน้ารายการ) ไฟล์เป็น UTF-8 พร้อมหัวคอลัมน์ภาษาไทย เหมาะสำหรับเปิดด้วย Google Sheets หรือ Excel รุ่นใหม่

## แก้ปัญหา

- เข้าไม่ได้: ตรวจ ADMIN_TOKEN ใน Apps Script → Project Settings → Script properties
- หน้าเก่าหรือข้อมูลไม่ใหม่: กดรีเฟรชหน้าเว็บ แล้วกด **รีเฟรช** ในแดชบอร์ด
- ไม่พบสลิป: ห้ามแก้ `payment_slip_file_id` ในชีตเอง ให้ตรวจว่าบัญชีเจ้าหน้าที่ได้รับสิทธิ์ Drive แล้ว
- อย่าแก้สถานะในชีตด้วยมือ ให้ใช้ปุ่มใน Admin เพื่อรักษาโควตาและ Audit Log
